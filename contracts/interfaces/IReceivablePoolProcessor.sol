// SPDX-License-Identifier: AGPL-3.0-or-later
pragma solidity ^0.8.0;

/**
 * @notice Interface for contracts that can record pre-approved credit request
 */
interface IReceivablePoolHandler {
    // function beforeDrawdown(bytes calldata parameters) external returns (bytes memory results);
    // function afterDrawdown(bytes calldata parameters) external returns (bytes memory results);
}
